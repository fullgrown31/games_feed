<h2>All Platforms</h2>
<ul>
    <% @platforms.each do |platform| %>
        <li><%= link_to platform.name, platform %> has <%= pluralize(platform.games.count, 'game') %></li>
    <% end %>
</ul>

<%= will_paginate @platforms %>